<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mylenovo.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.RegistreerActivity" />
        <activity android:name=".Activities.GarderobeActivity" />
        <activity android:name=".Activities.WishlistActivity" />
        <activity android:name=".Activities.LookbookActivity" />
        <activity android:name=".Activities.NieuwItemActivity" />
        <activity android:name=".Activities.OutfitActivity" />
        <activity android:name=".Activities.ItemActivity" />
        <activity android:name=".Activities.OutfitItemActivity" />
        <activity android:name=".Activities.AanpassenItemActivity"></activity>
    </application>

</manifest>